# Dual Pane Sync View - 双栏同步阅读插件

一个为 Obsidian 设计的双栏同步阅读插件，让你在大屏幕上更高效地阅读长文档。

## ✨ 功能特性

### 🖥️ 双栏连续阅读
- 左右两栏在同一标签页中显示文档的连续内容
- **阅读模式**: 左栏显示 1-26 行，右栏精确接续显示 27-52 行
- **编辑模式**: 双栏编辑器同步滚动，保持内容接续
- 充分利用超宽屏/4K显示器的横向空间

### 🔄 智能滚动同步
- 左栏滚动时，右栏自动显示接续的内容
- 无重叠、无遗漏的内容拼接
- 可在设置中开关同步功能

### ⌨️ 快捷键翻页
- `Page Down` / `空格键`: 下一页
- `Page Up`: 上一页
- 支持两种翻页模式：
  - **单栏翻页**: 右侧内容移到左侧，保持连续阅读
  - **双栏翻页**: 跳过一整屏内容，快速浏览长文档

### 📝 编辑模式支持

**状态关系设计（符合 Obsidian 原生逻辑）**:
- **阅读** 和 **编辑** 是两种互斥的主状态
- **源码模式** 是编辑模式下的一个开关选项（点击"源码"按钮在编辑状态下切换）

**三种编辑状态**:
- **阅读模式**: Markdown 渲染预览
- **编辑模式**: 双栏 textarea 实时编辑（两侧同步编辑，自动保存）
- **源码模式**: 编辑模式下开启，纯文本 Markdown 源码编辑

### 📁 文件事件监听
- 文件重命名时自动更新视图
- 文件删除时自动清空视图
- 文件修改时自动刷新内容（可选）

### ⚙️ 可自定义设置
- 滚动同步开关
- 翻页模式（单栏/双栏）
- 工具栏显示/隐藏
- 键盘导航开关
- 自动刷新开关

## 📥 安装方法

### 方法1：手动安装

1. 在你的 Obsidian Vault 中创建插件目录：
   ```
   <你的Vault>/.obsidian/plugins/obsidian-view-sync/
   ```

2. 将以下文件复制到该目录：
   - `main.js` (构建产物)
   - `manifest.json`
   - `styles.css`

3. 在 Obsidian 中启用插件：
   - 打开设置 → 第三方插件
   - 关闭安全模式（如果提示）
   - 开启 "Dual Pane Sync View"

### 方法2：开发模式安装

1. 克隆或下载本项目到本地
2. 进入项目目录并安装依赖：
   ```bash
   cd obsidian_view_sync
   npm install
   ```
3. 构建项目：
   ```bash
   npm run build
   ```
4. 创建符号链接到 Vault 的插件目录

## 🚀 使用方法

### 打开双栏视图

**方式一：功能区图标**
- 点击左侧功能区 📑 (columns) 图标

**方式二：命令面板**
- `Ctrl+P` (或 `Cmd+P`) 打开命令面板
- 搜索 "打开双栏同步视图" 或 "在当前笔记打开双栏视图"

### 切换编辑模式

工具栏按钮布局：
```
[阅读] [编辑] [源码]    [上一页] [下一页]
```

- **阅读**: 切换到 Markdown 渲染预览（主状态）
- **编辑**: 切换到编辑模式（主状态）
- **源码**: 在编辑模式下切换源码显示开关（仅在编辑模式下有效）

**说明**: 
- 阅读/编辑按钮是互斥的（类似 Obsidian 原生的编辑/预览切换）
- 源码模式按钮是编辑模式下的附加选项
- 从阅读模式点击"源码"会自动切换到编辑模式并开启源码显示

### 翻页操作

| 操作 | 方式 |
|------|------|
| 下一页 | 点击"下一页"按钮 / PageDown / 空格 |
| 上一页 | 点击"上一页"按钮 / PageUp |

### 翻页模式说明

**单栏翻页模式**（默认）:
```
初始: 左[1-26] 右[27-52]
翻页: 左[27-52] 右[53-78]  ← 右侧内容移到左侧，接续阅读
```

**双栏翻页模式**:
```
初始: 左[1-26] 右[27-52]
翻页: 左[53-78] 右[79-104]  ← 跳过一整屏，快速浏览
```

## ⚙️ 设置选项

进入 Obsidian 设置 → 第三方插件 → Dual Pane Sync View → 选项

| 选项 | 说明 | 默认值 |
|------|------|--------|
| 默认启用滚动同步 | 打开视图时是否启用内容接续 | `true` |
| 翻页模式 | 单栏翻页/双栏翻页 | `单栏翻页` |
| 显示工具栏 | 是否显示顶部工具栏 | `true` |
| 启用键盘导航 | 是否启用快捷键翻页 | `true` |
| 自动刷新 | 文件修改时是否自动刷新 | `true` |

## 📁 文件结构

```
obsidian_view_sync/
├── main.ts              # 主插件入口
├── main.js              # 编译后的插件
├── manifest.json        # 插件清单
├── styles.css           # 样式文件
├── src/
│   ├── dualPaneView.ts  # 双栏视图核心实现
│   ├── types.ts         # 类型定义
│   └── settings.ts      # 设置面板
├── package.json         # npm 配置
├── tsconfig.json        # TypeScript 配置
└── esbuild.config.mjs   # 构建配置
```

## 🔧 开发

### 环境要求
- Node.js 16+
- npm 或 yarn

### 安装依赖
```bash
npm install
```

### 开发模式（热重载）
```bash
npm run dev
```

### 生产构建
```bash
npm run build
```

## 🎯 技术实现

### 编辑模式下的内容拼接
编辑模式通过计算行高和滚动位置实现拼接：
```typescript
// 左栏显示从第0行开始
leftEditor.scrollTop = 0

// 右栏显示接续的内容（每栏显示N行）
linesPerPane = paneHeight / lineHeight
rightEditor.scrollTop = linesPerPane * lineHeight
```

### 翻页模式实现
```typescript
// 单栏翻页
newLeftLine = currentLeftLine + linesPerPane

// 双栏翻页
newLeftLine = currentLeftLine + linesPerPane * 2
```

## ✅ 兼容性

- **Obsidian 版本**: v0.15.0+
- **平台**: 桌面端 & 移动端（桌面端体验更佳）
- **主题**: 适配所有官方主题和大多数社区主题

## 🗺️ 未来计划

- [ ] 使用 CodeMirror 6 替代 textarea 提供更完善的编辑体验
- [ ] 支持自定义栏宽比例
- [ ] 双栏对比模式（对比两个文档）
- [ ] 记住每个文档的阅读位置
- [ ] 支持 PDF 文件的双栏阅读

## 📝 更新日志

### v1.2.0
- ✨ 编辑/源码模式内容拼接实现
- ✨ 编辑区域高度填满整个面板
- ✨ 重新设计阅读/编辑/源码按钮关系（源码作为编辑的子选项）
- ✨ 添加翻页模式自定义设置（单栏/双栏翻页）
- ✨ 修复按钮间距问题

### v1.1.0
- ✨ 在主窗口标签页中打开（替代侧边栏）
- ✨ 实现精确的内容拼接（左栏底部 = 右栏顶部）
- ✨ 无缝翻页逻辑（无重复无遗漏）
- ✨ 支持阅读/编辑/源码三种模式

### v1.0.0
- ✨ 初始版本发布

## 📄 许可证

MIT License
