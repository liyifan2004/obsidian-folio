# Dual Pane Sync View - 双栏同步阅读插件

一个为 Obsidian 设计的双栏同步阅读插件，让你在大屏幕上更高效地阅读长文档。

## ✨ 功能特性

### 🖥️ 双栏连续阅读
- 左右两栏在同一标签页中显示文档的连续内容
- **左栏显示 1-26 行，右栏精确接续显示 27-52 行**
- 翻页后左栏从 53 行开始，实现完美衔接
- 充分利用超宽屏/4K显示器的横向空间

### 🔄 智能滚动同步
- 左栏滚动时，右栏自动显示接续的内容
- 无重叠、无遗漏的内容拼接
- 可在工具栏或设置中开关同步功能

### ⌨️ 快捷键翻页
- `Page Down` / `空格键`: 下一页
- `Page Up`: 上一页
- 可在设置中启用/禁用键盘导航

### 📝 三种编辑模式无缝切换
- **阅读模式**: Markdown 渲染预览
- **编辑模式**: 双栏同步编辑（两侧同时编辑）
- **源码模式**: 纯文本 Markdown 源码编辑
- 打开双栏视图时自动匹配当前笔记的编辑状态

### 📁 文件事件监听
- 文件重命名时自动更新视图
- 文件删除时自动清空视图
- 文件修改时自动刷新内容（可选）

### ⚙️ 可自定义设置
- 滚动同步开关
- 工具栏显示/隐藏
- 键盘导航开关
- 自动刷新开关

## 📥 安装方法

### 方法1：手动安装

1. 在你的 Obsidian Vault 中创建插件目录：
   ```
   <你的Vault>/.obsidian/plugins/obsidian-view-sync/
   ```

2. 将以下文件复制到该目录：
   - `main.js` (构建产物)
   - `manifest.json`
   - `styles.css`

3. 在 Obsidian 中启用插件：
   - 打开设置 → 第三方插件
   - 关闭安全模式（如果提示）
   - 开启 "Dual Pane Sync View"

### 方法2：开发模式安装

1. 克隆或下载本项目到本地
2. 进入项目目录并安装依赖：
   ```bash
   cd obsidian_view_sync
   npm install
   ```
3. 构建项目：
   ```bash
   npm run build
   ```
4. 创建符号链接到 Vault 的插件目录

## 🚀 使用方法

### 打开双栏视图

**方式一：功能区图标**
- 点击左侧功能区 📑 (columns) 图标

**方式二：命令面板**
- `Ctrl+P` (或 `Cmd+P`) 打开命令面板
- 搜索 "打开双栏同步视图" 或 "在当前笔记打开双栏视图"

### 切换编辑模式

在双栏视图的工具栏中，点击对应按钮切换：
- **阅读**: Markdown 渲染预览
- **编辑**: 实时编辑模式
- **源码**: 纯文本编辑

### 翻页操作

| 操作 | 方式 |
|------|------|
| 下一页 | 点击"下一页"按钮 / PageDown / 空格 |
| 上一页 | 点击"上一页"按钮 / PageUp |
| 切换同步 | 设置中的"滚动同步"选项 |

### 翻页逻辑说明

假设文档总高度为 H，单栏可视高度为 h：
- **初始状态**: 左栏显示 `0 ~ h`，右栏显示 `h ~ 2h`
- **翻下一页**: 左栏显示 `h ~ 2h`（原右栏内容），右栏显示 `2h ~ 3h`
- **翻上一页**: 左栏显示 `-h ~ 0`（即 `0 ~ h` 的上一页），右栏相应变化

**关键特性**: 翻页时内容完美衔接，没有任何重复或遗漏！

## ⚙️ 设置选项

进入 Obsidian 设置 → 第三方插件 → Dual Pane Sync View → 选项

| 选项 | 说明 | 默认值 |
|------|------|--------|
| 默认启用滚动同步 | 打开视图时是否启用内容接续 | `true` |
| 显示工具栏 | 是否显示顶部工具栏 | `true` |
| 启用键盘导航 | 是否启用快捷键翻页 | `true` |
| 自动刷新 | 文件修改时是否自动刷新 | `true` |

## 📁 文件结构

```
obsidian_view_sync/
├── main.ts              # 主插件入口
├── main.js              # 编译后的插件
├── manifest.json        # 插件清单
├── styles.css           # 样式文件
├── src/
│   ├── dualPaneView.ts  # 双栏视图核心实现
│   ├── types.ts         # 类型定义
│   └── settings.ts      # 设置面板
├── package.json         # npm 配置
├── tsconfig.json        # TypeScript 配置
└── esbuild.config.mjs   # 构建配置
```

## 🔧 开发

### 环境要求
- Node.js 16+
- npm 或 yarn

### 安装依赖
```bash
npm install
```

### 开发模式（热重载）
```bash
npm run dev
```

### 生产构建
```bash
npm run build
```

## 🎯 技术实现

### 核心改进

#### 1. 在主窗口打开
使用 `workspace.getLeaf('tab')` 在主编辑区的标签页中打开视图，而不是侧边栏。

#### 2. 精确内容拼接
通过控制左右栏的 `scrollTop` 实现内容接续：
```
右栏 scrollTop = 左栏 scrollTop + 左栏高度
```

#### 3. 无缝翻页
翻页时新的左栏顶部 = 原左栏底部，实现完美衔接。

#### 4. 编辑模式支持
使用 textarea 组件实现编辑功能，两侧内容实时同步，修改自动保存到文件。

## ✅ 兼容性

- **Obsidian 版本**: v0.15.0+
- **平台**: 桌面端 & 移动端（桌面端体验更佳）
- **主题**: 适配所有官方主题和大多数社区主题

## 🗺️ 未来计划

- [ ] 使用 CodeMirror 6 替代 textarea 提供更完善的编辑体验
- [ ] 支持自定义栏宽比例
- [ ] 双栏对比模式（对比两个文档）
- [ ] 记住每个文档的阅读位置
- [ ] 支持 PDF 文件的双栏阅读
- [ ] 鼠标滚轮平滑滚动同步

## 📝 更新日志

### v1.1.0
- ✨ 在主窗口标签页中打开（替代侧边栏）
- ✨ 实现精确的内容拼接（左栏底部 = 右栏顶部）
- ✨ 无缝翻页逻辑（无重复无遗漏）
- ✨ 支持阅读/编辑/源码三种模式
- ✨ 编辑模式下两侧内容实时同步

### v1.0.0
- ✨ 初始版本发布
- ✨ 双栏同步阅读功能
- ✨ 文件事件监听
- ✨ 可自定义设置面板

## 📄 许可证

MIT License
