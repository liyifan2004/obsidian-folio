# Dual Pane Sync View - 双栏同步阅读插件

一个为 Obsidian 设计的双栏同步阅读插件，提供**两种模式**满足不同的使用场景。

## ✨ 两种双栏模式

### 模式一：跟随模式（可编辑+阅读）

**核心特点**：左侧可编辑，右侧自动跟随显示接续内容

```
┌─────────────────────────┬─────────────────────────┐
│      左侧：原生编辑器    │     右侧：接续预览      │
│   (可编辑，完全原生)    │   (预览模式，只读)      │
│                         │                         │
│  • 所有快捷键正常工作   │  • 自动跟随左侧滚动     │
│  • 所有插件兼容         │  • 左侧底部=右侧顶部    │
│  • Vim 模式正常         │  • 支持快捷键翻页       │
│  • 实时编辑同步         │  • 关闭时自动退出       │
└─────────────────────────┴─────────────────────────┘
```

**如何使用**：
1. 打开任意 Markdown 文件
2. 点击左侧 📑 图标 → "启动跟随模式（可编辑）"
3. 在左侧滚动时，右侧会自动跟随显示接续的内容
4. **支持快捷键翻页**（需在设置中配置）

**快捷键翻页**（在设置中自定义）：
- 上一页/下一页：单页翻页
- 连翻两页：快速浏览长文档

---

### 模式二：独立双栏视图（仅阅读）

**核心特点**：纯阅读模式，优化滚动流畅度

```
┌─────────────────────────┬─────────────────────────┐
│       左侧：阅读区       │       右侧：接续区       │
│      （仅阅读模式）      │      （仅阅读模式）      │
│                         │                         │
│  • Markdown 渲染        │  • 显示接续内容         │
│  • 流畅滚动同步         │  • 跟随左侧滚动         │
│  • 重合行数可调         │  • 无编辑功能           │
└─────────────────────────┴─────────────────────────┘
```

---

## 🆕 设置页面重构

设置面板现在分为三个部分：

### 1. 通用设置
- **重合行数**：调整左右两栏重合显示的行数（解决工具栏遮挡问题）

### 2. 独立双栏视图设置（仅阅读模式）
- 启用滚动同步
- 翻页模式（单栏/双栏）
- 显示工具栏
- 自动刷新

### 3. 跟随模式设置（可编辑模式）
- **快捷键设置**：
  - 上一页快捷键
  - 下一页快捷键
  - 连翻两页（上）
  - 连翻两页（下）

**设置快捷键**：
1. 点击输入框
2. 按下想要的快捷键组合（如 `Ctrl+Shift+↑`）
3. 快捷键会自动保存

---

## 🚀 使用方法

### 功能区图标

点击左侧功能区 📑 图标，弹出菜单：
- **打开独立双栏视图（仅阅读）** - 纯阅读模式
- **启动跟随模式（可编辑）** - 左侧可编辑，右侧跟随

### 命令面板

按 `Ctrl+P` 打开命令面板：
- `打开独立双栏视图`
- `启动跟随模式`
- `停止跟随模式`
- `跟随模式: 上一页/下一页`
- `跟随模式: 连翻两页`

---

## 🎯 本次更新内容

### v3.2.0
- ✅ 修复跟随模式编辑视图的滚动同步
- ✅ 移除跟随模式右侧窗口的多余竖线
- ✅ 提升独立双栏视图滚动流畅度（使用 requestAnimationFrame）
- ✅ 重构设置页面，分模式呈现设置项
- ✅ 为跟随模式添加快捷键翻页功能（支持连翻两页）

---

## 📥 安装方法

1. 创建插件目录：
   ```
   <你的Vault>/.obsidian/plugins/obsidian-view-sync/
   ```

2. 复制文件：
   - `main.js`
   - `manifest.json`
   - `styles.css`

3. 在 Obsidian 中启用插件

---

## 📄 许可证

MIT License
