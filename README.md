# Dual Pane Sync View - 双栏同步阅读插件

一个为 Obsidian 设计的双栏同步阅读插件。

## ✨ 跟随模式（推荐）

**核心特性**：
1. **双向滚动同步**：左侧滚动→右侧跟随，右侧滚动→左侧跟随
2. **模式自适应**：左侧切换编辑/预览模式时，跟随不会失效
3. **视图一致**：右侧默认视图与左侧保持一致（编辑↔编辑，预览↔预览）
4. **快捷键翻页**：支持自定义快捷键翻页

```
┌─────────────────────────┬─────────────────────────┐
│      左侧窗口            │      右侧窗口           │
│   (编辑或预览模式)       │   (与左侧同模式)        │
│                         │                         │
│  • 可切换编辑/预览       │  • 自动跟随左侧滚动     │
│  • 滚动时右侧跟随        │  • 滚动时左侧跟随       │
│  • 支持快捷键翻页        │  • 支持快捷键翻页       │
│  • 完全原生编辑体验      │  • 接续显示内容         │
└─────────────────────────┴─────────────────────────┘
```

**使用场景**：
- 需要正常编辑文档，同时查看接续内容
- 需要频繁切换编辑/预览模式
- 需要双向滚动浏览长文档

**启动方式**：
1. 打开任意 Markdown 文件（编辑或预览模式均可）
2. 点击 📑 图标 → "启动跟随模式（可编辑）"
3. 右侧会以**相同模式**打开同一文件
4. 左右两侧滚动时互相跟随

---

## 📖 独立双栏视图（仅阅读）

纯阅读模式，左右双栏显示接续内容，支持翻页按钮。

**使用场景**：专注阅读，不需要编辑功能

---

## 🔄 双向滚动说明

跟随模式现在支持**双向滚动**：
- **左→右**：左侧滚动时，右侧显示接续内容（左侧底部 = 右侧顶部）
- **右→左**：右侧滚动时，左侧显示前置内容（右侧顶部 = 左侧底部）

这意味着你可以从任意一侧控制阅读位置，两侧始终保持内容接续。

---

## ⚙️ 设置选项

### 通用设置
- **重合行数**：调整左右两栏重合显示的行数

### 跟随模式设置
- **快捷键**：自定义翻页快捷键（上一页、下一页、连翻两页）

### 独立双栏设置
- **翻页模式**：单栏翻页/双栏翻页
- **显示工具栏**：是否显示顶部工具栏

---

## 🚀 使用方法

### 功能区图标
点击左侧 📑 图标，弹出菜单选择模式。

### 命令面板
- `启动跟随模式` - 可编辑模式，双向滚动
- `停止跟随模式`
- `打开独立双栏视图` - 仅阅读模式

---

## 📥 安装方法

1. 复制文件到插件目录：
   ```
   <你的Vault>/.obsidian/plugins/obsidian-view-sync/
   ```
   - `main.js`
   - `manifest.json`
   - `styles.css`

2. 在 Obsidian 中启用插件

---

## 📝 更新日志

### v4.0.0
- ✅ **双向滚动同步**：右侧滚动时左侧也跟随
- ✅ **模式切换自适应**：左侧切换编辑/预览模式时跟随不失效
- ✅ **视图保持一致**：右侧默认与左侧同模式
- ✅ 增强滚动流畅度

### v3.2.0
- ✅ 跟随模式编辑视图支持
- ✅ 快捷键翻页功能

---

## 💡 关于独立双栏视图

现在跟随模式已经完美实现了双向滚动和模式切换，独立双栏视图的功能（纯阅读、翻页）都可以被跟随模式替代。**建议优先使用跟随模式**，因为它：
- 支持编辑和预览
- 支持双向滚动
- 支持快捷键翻页
- 体验更完整

独立双栏视图保留作为简化版阅读模式的选择。

---

## 📄 许可证

MIT License
