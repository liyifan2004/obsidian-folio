# Dual Pane Sync View - 双栏同步阅读插件

一个为 Obsidian 设计的双栏同步阅读插件，提供**两种模式**满足不同的使用场景。

## ✨ 两种双栏模式

### 模式一：跟随模式（推荐用于编辑）

**核心特点**：左侧完全原生，右侧跟随显示

```
┌─────────────────────────┬─────────────────────────┐
│      左侧：原生编辑器    │     右侧：接续预览      │
│   (Obsidian 原生体验)   │   (显示下一屏内容)      │
│                         │                         │
│  • 所有快捷键正常工作   │  • 自动跟随左侧滚动     │
│  • 所有插件兼容         │  • 只读显示             │
│  • Vim 模式正常         │  • 点击可交互           │
│  • 实时编辑同步         │  • 无翻页功能           │
└─────────────────────────┴─────────────────────────┘
```

**适用场景**：
- 需要正常使用 Obsidian 编辑功能
- 依赖各种编辑器插件（如 Vim 模式、表格编辑器）
- 需要频繁使用快捷键（Ctrl+B 加粗等）

**启动方式**：`Ctrl+P` → "双栏视图: 启动跟随模式"

---

### 模式二：独立双栏视图（推荐用于阅读）

**核心特点**：统一的自定义视图，左右两侧都是插件控制

```
┌─────────────────────────┬─────────────────────────┐
│       左侧：内容区       │       右侧：接续区       │
│                         │                         │
│  [阅读/编辑/源码] 模式  │  显示左侧底部接续内容   │
│  • 双栏同步滚动         │  • 同步滚动             │
│  • 支持翻页             │  • 支持翻页             │
│  • 独立编辑器           │  • 独立编辑器           │
└─────────────────────────┴─────────────────────────┘
```

**适用场景**：
- 专注阅读长文档
- 不需要复杂的编辑功能
- 想要统一的翻页体验

**启动方式**：`Ctrl+P` → "双栏视图: 打开独立双栏视图"

---

## 🚀 使用方法

### 跟随模式（编辑推荐）

1. 打开任意笔记
2. `Ctrl+P` → 搜索 "启动跟随模式"
3. 右侧会自动分割显示接续的内容
4. 在左侧正常编辑，右侧自动跟随滚动

**退出**：`Ctrl+P` → "停止跟随模式"，或直接关闭右侧标签页

### 独立双栏视图（阅读推荐）

1. `Ctrl+P` → 搜索 "打开独立双栏视图"
2. 进入双栏视图页面
3. 使用工具栏切换阅读/编辑/源码模式
4. 使用翻页按钮或自定义快捷键翻页

---

## 📝 功能特性

### 通用功能
- 双栏连续阅读，内容完美拼接
- 左栏滚动时右栏自动跟随
- 支持自定义快捷键

### 跟随模式特有
- 左侧完全使用 Obsidian 原生编辑器
- 所有快捷键、插件正常工作
- 编辑体验与平时完全一致
- 右侧仅作为接续预览

### 独立视图特有
- 三种模式切换：阅读 / 编辑 / 源码
- 支持单栏/双栏翻页模式
- 统一的工具栏控制
- 可自定义显示选项

---

## 📥 安装方法

### 手动安装

1. 在你的 Obsidian Vault 中创建插件目录：
   ```
   <你的Vault>/.obsidian/plugins/obsidian-view-sync/
   ```

2. 将以下文件复制到该目录：
   - `main.js`
   - `manifest.json`
   - `styles.css`

3. 在 Obsidian 中启用插件

---

## ⚙️ 设置选项

| 选项 | 说明 | 适用模式 |
|------|------|----------|
| 默认启用滚动同步 | 打开时是否启用内容接续 | 全部 |
| 翻页模式 | 单栏翻页/双栏翻页 | 独立视图 |
| 显示工具栏 | 是否显示顶部工具栏 | 独立视图 |
| 自动刷新 | 文件修改时自动刷新 | 独立视图 |

---

## ⌨️ 快捷键设置

在 Obsidian 设置 → 快捷键中，可以为以下命令绑定快捷键：

- `双栏视图: 下一页（仅独立模式）`
- `双栏视图: 上一页（仅独立模式）`
- `双栏视图: 切换跟随模式`

**注意**：不再预设 PageUp/PageDown/空格键，避免与编辑输入冲突。

---

## 🗺️ 模式选择指南

| 需求 | 推荐模式 | 原因 |
|------|----------|------|
| 需要 Vim 模式 | 跟随模式 | 左侧原生编辑器 |
| 需要快捷键加粗 | 跟随模式 | 左侧原生编辑器 |
| 需要表格插件 | 跟随模式 | 左侧原生编辑器 |
| 专注阅读长文 | 独立视图 | 更好的翻页体验 |
| 简单编辑 | 独立视图 | 统一的双栏编辑 |
| 对比阅读 | 跟随模式 | 原生预览更准确 |

---

## 📁 文件结构

```
obsidian_view_sync/
├── main.ts              # 主插件入口
├── main.js              # 编译后的插件
├── manifest.json        # 插件清单
├── styles.css           # 样式文件
├── src/
│   ├── dualPaneView.ts  # 独立双栏视图
│   ├── followMode.ts    # 跟随模式
│   ├── types.ts         # 类型定义
│   └── settings.ts      # 设置面板
└── ...
```

---

## 🔧 开发

```bash
npm install
npm run dev  # 开发模式
npm run build  # 生产构建
```

---

## 📝 更新日志

### v2.0.0
- ✨ 新增**跟随模式**：左侧原生编辑，右侧接续预览
- ✨ 两种模式可选，满足不同场景需求
- ✨ 移除默认键盘快捷键，支持自定义绑定
- ✨ 优化编辑体验，避免焦点丢失

### v1.2.0
- ✨ 编辑/源码模式内容拼接实现
- ✨ 添加翻页模式自定义设置
- ✨ 重新设计阅读/编辑/源码按钮关系

### v1.0.0
- ✨ 初始版本发布

---

## 📄 许可证

MIT License
