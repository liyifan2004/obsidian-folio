# Dual Pane Sync View - 双栏同步阅读插件

一个为 Obsidian 设计的双栏同步阅读插件，提供**两种模式**满足不同的使用场景。

## ✨ 两种双栏模式

### 模式一：跟随模式（推荐用于编辑+阅读）

**核心特点**：左侧可编辑，右侧自动跟随显示接续内容

```
┌─────────────────────────┬─────────────────────────┐
│      左侧：原生编辑器    │     右侧：接续预览      │
│   (可编辑，完全原生)    │   (预览模式，只读)      │
│                         │                         │
│  • 所有快捷键正常工作   │  • 自动跟随左侧滚动     │
│  • 所有插件兼容         │  • 显示接续内容         │
│  • Vim 模式正常         │  • 左侧底部=右侧顶部    │
│  • 实时编辑同步         │  • 关闭时自动退出       │
└─────────────────────────┴─────────────────────────┘
```

**如何使用**：
1. 打开任意 Markdown 文件
2. 点击左侧 📑 图标 → "启动跟随模式（可编辑）"
3. 右侧会分割显示同一个文件的预览
4. **在左侧滚动时，右侧会自动跟随**，显示接续的内容

**退出方式**：
- 关闭右侧标签页，自动退出
- 或关闭左侧标签页，自动退出

---

### 模式二：独立双栏视图（仅阅读）

**核心特点**：纯阅读模式，左右双栏显示接续内容

```
┌─────────────────────────┬─────────────────────────┐
│       左侧：阅读区       │       右侧：接续区       │
│      （仅阅读模式）      │      （仅阅读模式）      │
│                         │                         │
│  • Markdown 渲染        │  • 显示接续内容         │
│  • 支持翻页             │  • 跟随左侧滚动         │
│  • 重合行数可调         │  • 无编辑功能           │
└─────────────────────────┴─────────────────────────┘
```

**如何使用**：
1. 点击左侧 📑 图标 → "打开独立双栏视图（仅阅读）"
2. 进入纯阅读模式
3. 使用翻页按钮切换内容

---

## 🆕 重合行数设置

由于工具栏会遮挡部分内容，插件提供**重合行数**设置：

| 设置值 | 效果 |
|--------|------|
| 0 行 | 两侧紧密拼接 |
| 2 行 | 两侧有 2 行重合（推荐） |
| 3 行 | 两侧有 3 行重合 |

**示例（2 行重合）**：
```
左侧显示：第 1-12 行
右侧显示：第 11-22 行（11-12 行重合）
```

在设置面板中拖动滑块调整（0-10 行）。

---

## 🚀 使用方法

### 功能区图标

点击左侧功能区 📑 图标，弹出菜单：
- **打开独立双栏视图（仅阅读）** - 纯阅读模式
- **启动跟随模式（可编辑）** - 左侧可编辑，右侧跟随

### 命令面板

按 `Ctrl+P` 打开命令面板：
- `打开独立双栏视图` - 纯阅读模式
- `启动跟随模式` - 可编辑模式

---

## ⚙️ 设置选项

| 选项 | 说明 | 默认值 |
|------|------|--------|
| 重合行数 | 左右两栏重合显示的行数 | 2 行 |
| 默认启用滚动同步 | 打开时是否启用内容接续 | true |
| 翻页模式 | 单栏翻页/双栏翻页 | 单栏 |
| 显示工具栏 | 是否显示顶部工具栏 | true |
| 自动刷新 | 文件修改时自动刷新 | true |

---

## 🗺️ 模式选择指南

| 需求 | 推荐模式 | 原因 |
|------|----------|------|
| 需要编辑文档 | 跟随模式 | 左侧原生编辑器 |
| 需要 Vim 模式 | 跟随模式 | 完全原生支持 |
| 需要快捷键 | 跟随模式 | 所有快捷键正常 |
| 专注阅读 | 独立视图 | 纯粹的阅读体验 |

---

## 📥 安装方法

1. 创建插件目录：
   ```
   <你的Vault>/.obsidian/plugins/obsidian-view-sync/
   ```

2. 复制文件：
   - `main.js`
   - `manifest.json`
   - `styles.css`

3. 在 Obsidian 中启用插件

---

## 📝 更新日志

### v3.1.0
- ✅ 重新实现跟随模式滚动同步
- ✅ 优化滚动容器检测逻辑
- ✅ 使用 `requestAnimationFrame` 优化滚动性能

### v3.0.0
- ✅ 移除独立双栏的编辑功能
- ✅ 修复跟随模式自动关闭问题

### v2.0.0
- ✨ 新增跟随模式

### v1.0.0
- ✨ 初始版本

---

## 📄 许可证

MIT License
