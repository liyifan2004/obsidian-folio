# Dual Pane Sync View - 双栏同步阅读插件

一个为 Obsidian 设计的双栏同步阅读插件，提供**两种模式**满足不同的使用场景。

## ✨ 两种双栏模式

### 模式一：跟随模式（推荐用于编辑）

**核心特点**：左侧完全原生，右侧跟随显示

```
┌─────────────────────────┬─────────────────────────┐
│      左侧：原生编辑器    │     右侧：接续预览      │
│   (Obsidian 原生体验)   │   (预览模式，只读)      │
│                         │                         │
│  • 所有快捷键正常工作   │  • 自动跟随左侧滚动     │
│  • 所有插件兼容         │  • 显示接续内容         │
│  • Vim 模式正常         │  • 可点击交互           │
│  • 实时编辑同步         │  • 关闭时自动退出       │
└─────────────────────────┴─────────────────────────┘
```

**适用场景**：
- 需要正常使用 Obsidian 编辑功能
- 依赖各种编辑器插件（如 Vim 模式、表格编辑器）
- 需要频繁使用快捷键（Ctrl+B 加粗等）

**启动方式**：
- 点击左侧功能区 📑 图标 → 选择"启动跟随模式"
- 或 `Ctrl+P` → "启动跟随模式"

**退出方式**：
- 直接关闭右侧标签页，自动退出
- 或关闭左侧标签页，自动退出

---

### 模式二：独立双栏视图（推荐用于阅读）

**核心特点**：统一的自定义视图，左侧可编辑，右侧只读接续

```
┌─────────────────────────┬─────────────────────────┐
│       左侧：编辑区       │       右侧：接续区       │
│    （可编辑/可阅读）     │      （只读显示）        │
│                         │                         │
│  [阅读/编辑/源码] 模式  │  显示左侧底部接续内容   │
│  • 默认进入阅读模式     │  • 跟随左侧滚动         │
│  • 编辑时右栏实时更新   │  • 只读不可编辑         │
│  • 支持翻页             │                         │
└─────────────────────────┴─────────────────────────┘
```

**适用场景**：
- 专注阅读长文档
- 简单编辑需求
- 想要统一的翻页体验

**启动方式**：
- 点击左侧功能区 📑 图标 → 选择"打开独立双栏视图"
- 或 `Ctrl+P` → "打开独立双栏视图"

---

## 🆕 重合行数设置

**解决工具栏遮挡问题**

由于工具栏会遮挡部分内容，插件提供**重合行数**设置：

| 设置值 | 效果 |
|--------|------|
| 0 行 | 两侧紧密拼接，无重合 |
| 2 行 | 两侧有 2 行内容重合（推荐） |
| 3 行 | 两侧有 3 行内容重合 |

**示例（2 行重合）**：
```
左侧显示：第 1-12 行
右侧显示：第 11-22 行（其中 11-12 行与左侧重合）
```

在设置面板中拖动滑块调整重合行数（0-10 行）。

---

## 🚀 使用方法

### 功能区图标

点击左侧功能区 📑 图标，弹出菜单选择：
- **打开独立双栏视图** - 进入独立双栏模式
- **启动跟随模式** - 进入跟随模式（需要先打开文件）

### 命令面板

按 `Ctrl+P` 打开命令面板，搜索：
- `打开独立双栏视图`
- `启动跟随模式`

### 跟随模式（编辑推荐）

1. 打开任意笔记
2. 点击功能区 📑 图标，选择"启动跟随模式"
3. 右侧会以**预览模式**自动分割显示接续的内容
4. 在左侧正常编辑，右侧自动跟随滚动
5. 关闭右侧标签页即可退出

### 独立双栏视图（阅读推荐）

1. 点击功能区 📑 图标，选择"打开独立双栏视图"
2. 默认进入**阅读模式**
3. 使用工具栏切换阅读/编辑/源码模式
4. 编辑时只有左侧可编辑，右侧只读显示接续内容

---

## 📝 功能特性

### 跟随模式
- ✅ 左侧完全使用 Obsidian 原生编辑器
- ✅ 右侧为**预览模式**（只读）
- ✅ 所有快捷键、插件正常工作
- ✅ 左侧滚动时右侧**自动跟随**
- ✅ 关闭右侧自动退出

### 独立视图
- ✅ 默认进入**阅读模式**
- ✅ 阅读/编辑/源码三种模式
- ✅ 左栏可编辑，右栏只读接续
- ✅ 编辑时右栏实时更新
- ✅ 滚动同步（支持重合行数）

---

## ⚙️ 设置选项

| 选项 | 说明 | 默认值 |
|------|------|--------|
| 重合行数 | 左右两栏重合显示的行数 | 2 行 |
| 默认启用滚动同步 | 打开时是否启用内容接续 | true |
| 翻页模式 | 单栏翻页/双栏翻页 | 单栏 |
| 显示工具栏 | 是否显示顶部工具栏 | true |
| 自动刷新 | 文件修改时自动刷新 | true |

---

## 🗺️ 模式选择指南

| 需求 | 推荐模式 | 原因 |
|------|----------|------|
| 需要 Vim 模式 | 跟随模式 | 左侧原生编辑器 |
| 需要快捷键加粗 | 跟随模式 | 左侧原生编辑器 |
| 需要表格插件 | 跟随模式 | 左侧原生编辑器 |
| 专注阅读长文 | 独立视图 | 更好的翻页体验 |
| 简单编辑 | 独立视图 | 统一的双栏体验 |
| 对比阅读 | 跟随模式 | 原生预览更准确 |

---

## 📥 安装方法

### 手动安装

1. 在你的 Obsidian Vault 中创建插件目录：
   ```
   <你的Vault>/.obsidian/plugins/obsidian-view-sync/
   ```

2. 将以下文件复制到该目录：
   - `main.js`
   - `manifest.json`
   - `styles.css`

3. 在 Obsidian 中启用插件

---

## 📁 文件结构

```
obsidian_view_sync/
├── main.ts              # 主插件入口
├── main.js              # 编译后的插件
├── manifest.json        # 插件清单
├── styles.css           # 样式文件
├── src/
│   ├── dualPaneView.ts  # 独立双栏视图
│   ├── followMode.ts    # 跟随模式
│   ├── types.ts         # 类型定义
│   └── settings.ts      # 设置面板
└── ...
```

---

## 🔧 开发

```bash
npm install
npm run dev  # 开发模式
npm run build  # 生产构建
```

---

## 📝 更新日志

### v2.2.0
- ✅ 修复跟随模式滚动同步问题
- ✅ 跟随模式右侧改为只读预览
- ✅ 修复独立双栏编辑中断问题（输入时不自动保存）
- ✅ 独立双栏默认进入阅读模式
- ✅ 添加重合行数设置（解决工具栏遮挡）

### v2.1.0
- ✅ 新增跟随模式
- ✅ 简化命令菜单

### v2.0.0
- ✨ 两种双栏模式

### v1.0.0
- ✨ 初始版本

---

## 📄 许可证

MIT License
