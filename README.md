# Dual Pane Sync View - 双栏同步阅读插件

一个为 Obsidian 设计的双栏同步阅读插件，提供**两种模式**满足不同的使用场景。

## ✨ 两种双栏模式

### 模式一：跟随模式（推荐用于编辑+阅读）

**核心特点**：左侧可编辑，右侧自动跟随显示接续内容

```
┌─────────────────────────┬─────────────────────────┐
│      左侧：原生编辑器    │     右侧：接续预览      │
│   (可编辑，完全原生)    │   (预览模式，只读)      │
│                         │                         │
│  • 所有快捷键正常工作   │  • 自动跟随左侧滚动     │
│  • 所有插件兼容         │  • 显示接续内容         │
│  • Vim 模式正常         │  • 可点击交互           │
│  • 实时编辑同步         │  • 关闭时自动退出       │
└─────────────────────────┴─────────────────────────┘
```

**适用场景**：
- 需要正常编辑文档
- 依赖各种编辑器插件（如 Vim 模式）
- 需要频繁使用快捷键
- 一边编辑一边查看接续内容

**启动方式**：
- 点击左侧功能区 📑 图标 → 选择"启动跟随模式（可编辑）"
- 或 `Ctrl+P` → "启动跟随模式"

**退出方式**：
- 关闭右侧标签页，自动退出
- 或关闭左侧标签页，自动退出

---

### 模式二：独立双栏视图（仅阅读）

**核心特点**：纯阅读模式，无编辑功能

```
┌─────────────────────────┬─────────────────────────┐
│       左侧：阅读区       │       右侧：接续区       │
│      （仅阅读模式）      │      （仅阅读模式）      │
│                         │                         │
│  • Markdown 渲染        │  • 显示接续内容         │
│  • 支持翻页             │  • 跟随左侧滚动         │
│  • 重合行数可调         │  • 无编辑功能           │
└─────────────────────────┴─────────────────────────┘
```

**适用场景**：
- 专注阅读长文档
- 不需要编辑功能
- 想要纯粹的阅读体验

**启动方式**：
- 点击左侧功能区 📑 图标 → 选择"打开独立双栏视图（仅阅读）"
- 或 `Ctrl+P` → "打开独立双栏视图"

---

## 🆕 重合行数设置

由于工具栏会遮挡部分内容，插件提供**重合行数**设置：

| 设置值 | 效果 |
|--------|------|
| 0 行 | 两侧紧密拼接 |
| 2 行 | 两侧有 2 行重合（推荐） |
| 3 行 | 两侧有 3 行重合 |

**示例（2 行重合）**：
```
左侧显示：第 1-12 行
右侧显示：第 11-22 行（11-12 行重合）
```

在设置面板中拖动滑块调整（0-10 行）。

---

## 🚀 使用方法

### 功能区图标

点击左侧功能区 📑 图标，弹出菜单选择：
- **打开独立双栏视图（仅阅读）** - 进入纯阅读模式
- **启动跟随模式（可编辑）** - 左侧可编辑，右侧跟随

### 命令面板

按 `Ctrl+P` 打开命令面板，搜索：
- `打开独立双栏视图` - 纯阅读模式
- `启动跟随模式` - 可编辑模式

---

## ⚙️ 设置选项

| 选项 | 说明 | 默认值 |
|------|------|--------|
| 重合行数 | 左右两栏重合显示的行数 | 2 行 |
| 默认启用滚动同步 | 打开时是否启用内容接续 | true |
| 翻页模式 | 单栏翻页/双栏翻页 | 单栏 |
| 显示工具栏 | 是否显示顶部工具栏 | true |
| 自动刷新 | 文件修改时自动刷新 | true |

---

## 🗺️ 模式选择指南

| 需求 | 推荐模式 | 原因 |
|------|----------|------|
| 需要编辑文档 | 跟随模式 | 左侧原生编辑器 |
| 需要 Vim 模式 | 跟随模式 | 完全原生支持 |
| 需要快捷键 | 跟随模式 | 所有快捷键正常 |
| 专注阅读 | 独立视图 | 纯粹的阅读体验 |
| 不需要编辑 | 独立视图 | 无编辑干扰 |

---

## 📥 安装方法

1. 创建插件目录：
   ```
   <你的Vault>/.obsidian/plugins/obsidian-view-sync/
   ```

2. 复制文件：
   - `main.js`
   - `manifest.json`
   - `styles.css`

3. 在 Obsidian 中启用插件

---

## 📝 更新日志

### v3.0.0
- ✅ 移除独立双栏的编辑功能（解决编辑中断问题）
- ✅ 修复跟随模式自动关闭问题
- ✅ 简化插件定位：跟随模式（可编辑）+ 独立视图（仅阅读）
- ✅ 优化跟随模式检测逻辑

### v2.2.0
- ✅ 添加重合行数设置
- ✅ 修复跟随模式滚动同步

### v2.0.0
- ✨ 新增跟随模式

### v1.0.0
- ✨ 初始版本

---

## 📄 许可证

MIT License
